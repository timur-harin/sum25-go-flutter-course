name: Deploy Flutter Web to GitHub Pages

on:
  push:
    branches:
      - lab07-farafonov-stefan
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Install dependencies
        run: flutter pub get
        working-directory: ./labs/lab07/frontend

      - name: Build web
        run: flutter build web --base-href /sum25-go-flutter-course/
        working-directory: labs/lab07/frontend

      - name: List build output
        run: ls -l ./labs/lab07/frontend/build/web

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.TOKEN }}
          publish_dir: ./labs/lab07/frontend/build/web
          user_name: ${{ secrets.USER_NAME }}
          user_email: ${{ secrets.USER_EMAIL }}
          commit_message: "Deploy Flutter Web"
          